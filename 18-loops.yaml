- name: install packages using loops
  hosts: web
  become: yes

  tasks:
    - name: install packages
      ansible.builtin.dnf:
        name: "{{ item }}"
        state: installed
      loop:
        - nginx
        - git
        - gcc
        - mysql
        - zip